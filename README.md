# Syntax Highlighting Worker

WebWorker for the syntax highlighting functionality in Lvce Editor.

## Contributing

```sh
git clone git@github.com:lvce-editor/syntax-highlighting-worker.git &&
cd syntax-highlighting-worker &&
npm ci &&
npm test
```
